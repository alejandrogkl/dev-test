/* Rules:

Given an array of positive integers. Your task is to find the leaders in the array.
Note: An element of array is leader if it is greater than or equal to all the elements to its right side. Also, the rightmost element is always a leader.

Dado un array de números positivos enteros. La tarea es encontrar los líderes del array
Notas: Un elemento del array es líder si es mayor o igual que todos los elementos a su derecha. El elemento más a la derecha simpre es líder.

*/

/* Test:
ng test dev-test --test-file=leaders
*/

export function leaders_in_array(numbers: number[]): number[] {
  return [];
}
